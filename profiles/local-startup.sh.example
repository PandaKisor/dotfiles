#!/usr/bin/env bash

# Copy this to ~/.config/i3/local-startup.sh, make it executable, and tailor it
# to each machine. It is deliberately ignored by Git.

# Example physical-machine display configuration:
# xrandr --output HDMI-1 --mode 2560x1440 --rate 120 --scale 1x1
